'use strict';

gardnrControllers.controller( 'DashboardController', [ '$scope', '$http', 
	function( $scope, $http ) {
		// $http.get( 'data/categories.json' ).success( function( data ) {
		// 	$scope.categories = data;
		// } );
		// $http.get( 'data/plants.json' ).success( function( data ) {
		// 	$scope.plants = data;
		// } );
	} ] );